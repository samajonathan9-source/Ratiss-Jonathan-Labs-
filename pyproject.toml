[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ratiss-jonathan-labs"
version = "0.1.0"
description = "Reproducible RATISS graph-inspection and descriptive bio experiments."
requires-python = ">=3.11"
dependencies = ["numpy>=1.26"]
